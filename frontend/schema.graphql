type AuthInfo {
  token: String!
  user: User!
}

type Mutation {
  signIn(email: String!, password: String!): AuthInfo!
  signUp(
    email: String!
    gender: String!
    name: String!
    password: String!
    surname: String!
  ): AuthInfo!
}

type Query {
  _empty: String!
  user(id: String!): User
  users: [User!]!
}

type User {
  contactId: Float!
  id: ID!
  login: String!
  password: String!
}
