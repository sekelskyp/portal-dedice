import { defineStyleConfig } from '@chakra-ui/react'

export const Button = defineStyleConfig({
  baseStyle: {
    fontWeight: 'semibold',
    borderRadius: 'base',
  },
  variants: {},
  defaultProps: {
    colorScheme: 'primary',
  },
})
